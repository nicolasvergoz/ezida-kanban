# macOS
.DS_Store

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo

# Go build artifacts
/ezida
/dist/
/bin/
*.exe
*.test
*.out
*.tar.gz
checksums.txt
coverage.out
coverage.html

# Go module / vendor (repo-root only — embedded UI vendor stays tracked)
/vendor/

# Env / local config
.env
.env.local
*.local

# Logs
*.log

# Claude Code: personal workflow tooling (opsx commands + openspec skills)
.claude/commands/opsx/
.claude/skills/openspec-*/
.claude/settings.local.json

# Agent context refs (not for public repo)
refs/

# Generated demo snapshot (regenerated by the pages workflow from kanban.toml)
/site/demo/board.json
site/assets/screenshot-light.png

# Migration backups written by `ezida migrate`
kanban.toml.*.bak

# Node tooling for the browser tests (the Go module is the project)
/node_modules/
/e2e/.bin/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
