# No Spec Changes

This change is documentation-only: it relocates content between
`README.md` and a new `docs/` directory. No capability requirements
are added, modified, removed, or renamed.

The proposal's `Capabilities` section is intentionally empty for this
reason. This file exists solely to satisfy the OpenSpec
`specs/**/*.md` artifact gate and carries no normative content.
