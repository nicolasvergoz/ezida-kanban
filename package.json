{
  "name": "ezida-kanban-e2e",
  "private": true,
  "description": "Browser tests for the embedded viewer. Not published; the Go module is the project.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:visual": "PW_VISUAL=1 playwright test",
    "test:e2e:update-snapshots": "PW_VISUAL=1 playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
